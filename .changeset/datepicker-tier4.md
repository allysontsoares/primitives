---
"@kenos-ui/react-datepicker": minor
---

Tier 4 features: controlled `mode="multiple"` sync, `pageBehavior`, `allowsNonContiguousRanges`, `Presets` + `useDatePickerActions`, and time `granularity` via timescape segments.
